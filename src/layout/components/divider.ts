import { ComponentStyleConfig } from '@chakra-ui/react';

const dividerBase: ComponentStyleConfig = {
  baseStyle: {
    marginTop: '0 !important',
    borderColor: 'gray.600',
    boxShadow: 'dark-sm',
  },
};

export default dividerBase;
